window.addEventListener('DOMContentLoaded', (event) => {

    var year = 2022;

    //year % 4 === 0 && year % 100 > 0 || year % 400 === 0 ? console.log(year + ' est bissextile') : console.log(year + ' n\'est pas bissextile');

    /*if ((year % 4 === 0 && year % 100 > 0) || (year % 400 === 0)) {
        console.log('Oui');
    } else {
        console.log('Non');
    }*/

    /*var number = 42;

    var units = number % 10;
    var tens = (number - units) / 10; il suffit de soustraire les unités puis de diviser par 10.*/
});